version: '2'
services:
    node-app:
        build:
          context: .
          dockerfile: Dockerfile
        volumes:
            - ./nginx/nginx.conf:/etc/nginx/nginx.conf
        ports:
            - 80:80
            - 443:443
